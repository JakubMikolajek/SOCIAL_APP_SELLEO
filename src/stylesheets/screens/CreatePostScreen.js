import {StyleSheet} from "react-native";

export const createPostScreen = StyleSheet.create({
    container: {
        flex: 1
    },
    innerContainer:{
        flex:1,
        justifyContent:"center",
        alignItems:"center"
    }
})
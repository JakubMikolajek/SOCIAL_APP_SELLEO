import {StyleSheet} from "react-native";
import {GlobalStyles} from "../../../constants/GlobalStyles";

export const inputController = StyleSheet.create({
    errorMessage:{
        color: GlobalStyles.colors.error
    }
})